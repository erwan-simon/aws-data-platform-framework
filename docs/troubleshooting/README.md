# Troubleshooting

Errors whose meaning isn't obvious from the message alone — usually because the
framework's stage-prefixing or per-domain conventions are involved. One file per
topic; add an entry when you hit a new one.

## Entries

| Symptom                                                | Page                                                                            |
|--------------------------------------------------------|---------------------------------------------------------------------------------|
| `InvalidInputException: Database not found` at apply   | [lakeformation-database-not-found.md](lakeformation-database-not-found.md)      |
| Sandbox `docker build` aborts mid-`COPY` with a BuildKit `graceful_stop` / EOF | [docker-build-graceful-stop.md](docker-build-graceful-stop.md) |
